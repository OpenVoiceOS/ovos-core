name: Type Check

on:
  pull_request:
    branches: [dev]
  workflow_dispatch:

jobs:
  type_check:
    uses: OpenVoiceOS/gh-automations/.github/workflows/type-check.yml@dev
    secrets: inherit
    with:
      python_version: "3.11"
      pr_comment: true
